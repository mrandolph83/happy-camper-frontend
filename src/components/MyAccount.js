import React from 'react';

const MyAccount = () => (
    <div>
        <h2>User Info</h2>
        <p>User favorites on the left side in a list, 
            User into they can edit in a link on the top R
            User reviews listed belw that can be edited or deleted
        </p>
  
        </div>
);

export default MyAccount